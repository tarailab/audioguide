// Bump this on every change you want to verify on the device. It is compiled
// into the JS bundle, while public/version.json (same value) is fetched fresh on
// load. If they differ, the running bundle is stale and the app auto-reloads.
export const APP_VERSION = '2026-06-27.5';
